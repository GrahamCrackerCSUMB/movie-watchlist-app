<!DOCTYPE html>
<html>
<head><title>WatchList Home</title></head>
<body>
<h1>My Movie Watchlist</h1>
<nav>
    <a href="/search">Search Movies</a> |
    <a href="/watchlist">Full Watchlist</a> |
    <a href="/logout" style="color: red;">Logout</a>
</nav>

<h2>Quick View</h2>
<ul>
    <% if (watchlist.length === 0) { %>
        <li>Your watchlist is empty. <a href="/search">Add some movies!</a></li>
    <% } else { %>
        <% watchlist.forEach(item => { %>
            <li>
                <strong><%= item.title %></strong>
                (<%= item.watched_status ? 'Watched' : 'To Watch' %>)
            </li>
        <% }); %>
    <% } %>
</ul>

<%- include('partials/footer.ejs') %>
</body>
</html>